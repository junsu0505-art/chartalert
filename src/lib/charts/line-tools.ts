// TODO R5a: difurious line-tools wrapper (fallback if drawing 부족)
// lightweight-charts-line-tools@4.1.1 래퍼 — R5a 회차에서 구현 예정
// 설치 주의: --ignore-scripts 플래그 필수 (postinstall 깨짐)

/**
 * placeholder: line-tools 초기화
 * R5a 에서 실제 구현 대체
 */
export function initLineTools(_chartRef: unknown): void {
  // TODO R5a
}
